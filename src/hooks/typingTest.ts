import { typingTestTexts } from "src/constants/typingText"
import { typingTestStorage } from "src/stores/menuStore"

export const startTypingTest = () => {
    
    const {activeTypingTest} = typingTestStorage()

    //timer
    //active
    //wordsWrited
    //textToWrite
    //writedText
}